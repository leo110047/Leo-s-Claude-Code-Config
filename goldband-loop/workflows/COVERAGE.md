# Capability runtime coverage

The authoritative capability/action inventory and runtime maturity table is
generated from `goldband.manifest.json` at `docs/generated/capabilities.md`.

Use `node scripts/generate-goldband-surfaces.mjs --check` from the repository
root to verify freshness. Historical workflow names are intentionally absent
and are not supported aliases.
