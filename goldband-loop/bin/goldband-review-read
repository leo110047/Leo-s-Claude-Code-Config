#!/usr/bin/env bash
# goldband-review-read — read review log and config for dashboard
# Usage: goldband-review-read
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
eval "$("$SCRIPT_DIR/goldband-slug" 2>/dev/null)"
GOLDBAND_HOME="${GOLDBAND_HOME:-$HOME/.goldband}"
cat "$GOLDBAND_HOME/projects/$SLUG/$BRANCH-reviews.jsonl" 2>/dev/null || echo "NO_REVIEWS"
echo "---CONFIG---"
"$SCRIPT_DIR/goldband-config" get skip_eng_review 2>/dev/null || echo "false"
echo "---HEAD---"
git rev-parse --short HEAD 2>/dev/null || echo "unknown"
