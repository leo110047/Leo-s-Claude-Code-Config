---
description: Fallback adapter for the installed Goldband capability router.
argument-hint: <capability> <action>
---

# Goldband capability adapter

The installed `$goldband` skill is the primary Codex entrypoint. On surfaces
that still expand this prompt, resolve the installed `goldband` runtime and
follow its root `SKILL.md` using `<capability> <action>`.

Do not maintain a second catalog here. Historical workflow names are not
aliases; unknown input must fail explicitly.
